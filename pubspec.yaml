name: todaygoodwords
description: A new Flutter application.
publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  domain:
    path: domain
  firebase_repository:
    path: firebase_repository
  native_bloc:
    path: native_bloc
  filed_service:
    path: filed_service
  view:
    path: view
  firebase_core: ^1.6.0
  firebase_analytics: ^8.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  mockito: ^5.0.16
  build_runner: ^2.1.2
